#!/usr/bin/env sh

info() {
  printf '%s\n' "$1"
}

info "🧪 Pre-push: running service test suites..."
scripts/test-rust-services.sh

info "📝 Pre-push: checking repository formatting..."
bunx prettier --check "**/*.{md,yml,yaml,json}"

info "✅ Pre-push: all checks passed."
